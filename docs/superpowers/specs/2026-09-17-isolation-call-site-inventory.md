# Transaction call-site isolation inventory (2026-09-17, #906)

Companion to `2026-09-17-adr-transaction-isolation-opt-down.md`. Every `WithRetryable*Transaction` call site at main `f0f17a98`, classified against the ADR rules by four read-only review agents and spot-checked. Line numbers are as of that commit. `OPT_DOWN` sites run at READ COMMITTED after #906, except `api/client_credentials_service.go:101`, which was left alone because its closure ignores the transaction handle (#911). Everything else keeps the SERIALIZABLE default.

| file:line | function | writes (tables + verb) | reads that feed decisions | class | reason |
|---|---|---|---|---|---|
| api/tombstone_store.go:89 | GormThreatModelStore.SoftDelete | threat_models UPDATE deleted_at; threats/diagrams/documents/assets/notes/repositories UPDATE deleted_at | SELECT threat_model WHERE deleted_at IS NULL guards the cascade | KEEP | check-then-update of pre-existing rows across seven tables. |
| api/tombstone_store.go:142 | GormThreatModelStore.Restore | same seven tables UPDATE deleted_at=NULL | SELECT threat_model WHERE deleted_at IS NOT NULL guards the restore | KEEP | check-then-update of pre-existing rows. |
| api/tombstone_store.go:196 | GormThreatModelStore.hardDeleteTx | metadata/collaboration_sessions/threats/diagrams/documents/assets/notes/repositories/threat_model_access/threat_models DELETE | Pluck of child ids drives the metadata DELETE IN-lists | KEEP | multi-table cascading delete driven by ids read in the same tx. |
| api/tombstone_store.go:299 | GormDiagramStore.SoftDelete | diagrams UPDATE deleted_at; threats UPDATE diagram_id/cell_id=NULL | RowsAffected of the first UPDATE gates the not-found error | KEEP | updates two sets of pre-existing rows. |
| api/tombstone_store.go:329 | GormDiagramStore.Restore | diagrams UPDATE deleted_at=NULL | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:375 | GormDocumentRepository.SoftDelete | documents UPDATE deleted_at | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:405 | GormDocumentRepository.Restore | documents UPDATE deleted_at=NULL | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:453 | GormNoteRepository.SoftDelete | notes UPDATE deleted_at | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:479 | GormNoteRepository.Restore | notes UPDATE deleted_at=NULL | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:527 | GormRepositoryRepository.SoftDelete | repositories UPDATE deleted_at | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:553 | GormRepositoryRepository.Restore | repositories UPDATE deleted_at=NULL | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:598 | GormAssetRepository.SoftDelete | assets UPDATE deleted_at | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:621 | GormAssetRepository.Restore | assets UPDATE deleted_at=NULL | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:663 | GormThreatRepository.SoftDelete | threats UPDATE deleted_at | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/tombstone_store.go:710 | GormThreatRepository.BulkSoftDelete | threats UPDATE deleted_at (batch) | RowsAffected == len(ids) all-or-nothing check | KEEP | batch update of pre-existing rows with a multi-row invariant enforced in Go. |
| api/tombstone_store.go:741 | GormThreatRepository.Restore | threats UPDATE deleted_at=NULL | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/audit_store.go:133 | GormAuditService.RecordMutation | audit_entries INSERT; version_snapshots INSERT (createVersionSnapshot) | SELECT MAX(version) for this object, then nextVersion = max+1 | KEEP | textbook max+1 read feeding the inserted version number (and the checkpoint/diff branch). |
| api/audit_store.go:483 | GormAuditService.PruneAuditEntries | version_snapshots DELETE; audit_entries DELETE | entryIDs plucked before the tx drive both DELETE IN-lists | KEEP | deletes of pre-existing rows. |
| api/audit_store.go:588 | GormAuditService.PruneOrphanedVersionSnapshots | version_snapshots DELETE (raw SQL with NOT EXISTS) | correlated NOT EXISTS subquery decides which rows die | KEEP | delete whose row set is decided by a read of another table in the same statement. |
| api/audit_store.go:681 | GormAuditService.executePrune | version_snapshots DELETE (chunked) | Pluck of snapshot ids below the boundary inside the tx | KEEP | read-then-delete of pre-existing rows. |
| api/audit_store.go:918 | GormAuditService.PruneSystemAuditEntries | system_audit_entries DELETE (batch) | ids plucked before the tx | KEEP | delete of pre-existing rows. |
| api/database_store_gorm.go:951 | GormThreatModelStore.Create (runCreate/createTx) | threat_models INSERT; threat_model_access INSERT; metadata INSERT; alias_counters sequence/row-locked draw | user-identifier -> internal_uuid resolution only (FK lookups) | OPT_DOWN | ALREADY OPTED DOWN (#903 reference site): all writes are inserts keyed by the fresh id; only permitted shared write is the alias sequence/row-locked counter. |
| api/database_store_gorm.go:994 | GormThreatModelStore.update | threat_models UPDATE; threat_model_access replace; metadata write; version CAS | CheckAndBumpVersion CAS + SELECT existing row to decide status/status_updated | KEEP | optimistic-lock CAS plus read-derived status transition on a pre-existing row. |
| api/database_store_gorm.go:1811 | GormDiagramStore.CreateWithThreatModel | diagrams INSERT; alias_counters sequence/row-locked draw | none (alias allocation only) | OPT_DOWN | single INSERT keyed by a uuid.New() generated in this request, plus the permitted alias allocation; no read feeds a write decision. |
| api/database_store_gorm.go:1917 | GormDiagramStore.update | diagrams UPDATE; metadata update; version CAS | CheckAndBumpVersion CAS, RowsAffected gate | KEEP | CAS-guarded update of a pre-existing row. |
| api/database_store_gorm.go:1980 | GormDiagramStore.hardDeleteDiagram | threats UPDATE (nullify FKs); metadata DELETE; diagrams DELETE | RowsAffected gate | KEEP | cascading update+delete of pre-existing rows. |
| api/metadata_repository.go:93 | GormMetadataRepository.Create | metadata INSERT | none; duplicate detected from the DB unique-constraint error | OPT_DOWN | one INSERT with a fresh UUID; uniqueness enforced by the constraint, not by a read. |
| api/metadata_repository.go:190 | GormMetadataRepository.Update | metadata UPDATE value/modified_at | RowsAffected gate | KEEP | update of a pre-existing row. |
| api/metadata_repository.go:256 | GormMetadataRepository.Delete | metadata DELETE | RowsAffected gate | KEEP | delete of a pre-existing row. |
| api/metadata_repository.go:394 | GormMetadataRepository.BulkCreate | metadata INSERT (CreateInBatches) | Pluck of existing keys aborts with 409 before the insert | KEEP | check-then-insert: an existence read guards the write. |
| api/metadata_repository.go:484 | GormMetadataRepository.BulkUpdate | metadata MERGE/ON CONFLICT upsert (CreateInBatches) | none | OPT_DOWN | single-statement atomic upsert per batch with fresh UUIDs; no read feeds any write decision (rule 3 upsert case). |
| api/metadata_repository.go:585 | GormMetadataRepository.BulkReplace | metadata DELETE then INSERT | none | OPT_DOWN | ALREADY OPTED DOWN (#783 reference site, explicit &sql.TxOptions ReadCommitted); pure delete-then-insert, reads nothing it acts upon. |
| api/metadata_repository.go:635 | GormMetadataRepository.BulkDelete | metadata DELETE (key IN ?) | none | KEEP | deletes pre-existing rows. |
| api/threat_store_gorm.go:67 | GormThreatRepository.Create | threats INSERT; alias_counters sequence/row-locked draw | none | OPT_DOWN | one INSERT keyed by a UUIDv7 generated in this request plus the permitted alias allocation. |
| api/threat_store_gorm.go:197 | GormThreatRepository.update | threats UPDATE; version CAS | CheckAndBumpVersion CAS, RowsAffected gate | KEEP | CAS-guarded update of a pre-existing row. |
| api/threat_store_gorm.go:302 | GormThreatRepository.hardDeleteThreat | threats DELETE | RowsAffected gate | KEEP | delete of a pre-existing row. |
| api/threat_store_gorm.go:930 | GormThreatRepository.BulkCreate | threats INSERT (batch); alias_counters sequence/row-locked draw per row | none | OPT_DOWN | all writes are inserts with ids generated in this request; only shared write is the permitted alias allocation. |
| api/threat_store_gorm.go:981 | GormThreatRepository.BulkUpdate | threats UPDATE per row; metadata write per row (saveMetadataTx) | RowsAffected per row decides whether metadata is written (cross-tenant guard) | KEEP | updates pre-existing rows and branches a second write on a read result. |

| file:line | function | writes (tables + verb) | reads that feed decisions | class | reason (one sentence) |
|---|---|---|---|---|---|
| api/asset_store_gorm.go:64 | GormAssetRepository.Create | alias_counters upsert + FOR UPDATE increment (or sequence draw); assets INSERT | alias counter row read under row lock only | OPT_DOWN | Single INSERT keyed by a UUID generated in this request plus the row-locked/sequence alias allocation, both correct at READ COMMITTED. |
| api/asset_store_gorm.go:220 | GormAssetRepository.update | assets UPDATE (+ optional version CAS) | CheckAndBumpVersion reads and compares the current version | KEEP | UPDATE of a pre-existing row, optionally guarded by an optimistic-lock CAS. |
| api/asset_store_gorm.go:335 | GormAssetRepository.hardDeleteAsset | assets DELETE | none in closure | KEEP | Hard DELETE of a pre-existing row. |
| api/asset_store_gorm.go:490 | GormAssetRepository.BulkCreate | alias_counters upsert/increment per asset; assets bulk INSERT | alias counter rows under row lock only | OPT_DOWN | Only INSERTs of rows with UUIDs generated in this request plus row-locked alias allocations. |
| api/asset_store_gorm.go:780 | GormAssetRepository.saveMetadata | metadata DELETE then INSERT | none | KEEP | Delete-then-insert replacement of existing metadata rows. |
| api/webhook_store_gorm.go:310 | GormWebhookSubscriptionStore.Create | webhook_subscriptions INSERT | none in closure | OPT_DOWN | Closure is a single INSERT of a row whose UUID id is generated in this request, with no reads at all. |
| api/webhook_store_gorm.go:361 | GormWebhookSubscriptionStore.Update | webhook_subscriptions UPDATE | none in closure (RowsAffected only) | KEEP | UPDATE of a pre-existing row. |
| api/webhook_store_gorm.go:381 | GormWebhookSubscriptionStore.UpdateStatus | webhook_subscriptions UPDATE (status) | none in closure | KEEP | State transition UPDATE of a pre-existing row. |
| api/webhook_store_gorm.go:404 | GormWebhookSubscriptionStore.UpdateChallenge | webhook_subscriptions UPDATE (challenge, challenges_sent) | none in closure; challenges_sent computed by the caller in Go | KEEP | UPDATE of a pre-existing row with a caller-computed counter, verification-state sensitive. |
| api/webhook_store_gorm.go:428 | GormWebhookSubscriptionStore.UpdatePublicationStats | webhook_subscriptions UPDATE (last_successful_use/publication_failures) | none in closure | KEEP | UPDATE of a pre-existing row (success branch writes literal values, not an atomic expression). |
| api/webhook_store_gorm.go:457 | GormWebhookSubscriptionStore.IncrementTimeouts | webhook_subscriptions UPDATE `timeout_count + 1` | none | KEEP | Single UPDATE of a pre-existing row; conservative per the "a single UPDATE makes it KEEP" rule even though the expression is atomic. |
| api/webhook_store_gorm.go:478 | GormWebhookSubscriptionStore.ResetTimeouts | webhook_subscriptions UPDATE (timeout_count = 0) | none | KEEP | UPDATE of a pre-existing row. |
| api/webhook_store_gorm.go:498 | GormWebhookSubscriptionStore.Delete | webhook_subscriptions DELETE | none | KEEP | DELETE of a pre-existing row. |
| api/webhook_store_gorm.go:702 | GormWebhookQuotaStore.Create | webhook_quotas INSERT | none in closure | OPT_DOWN | Closure is a single INSERT with no reads; the owner_id primary key makes duplicate races a PK-constraint violation at any isolation level. |
| api/webhook_store_gorm.go:723 | GormWebhookQuotaStore.Update | webhook_quotas UPDATE (limits) | none in closure | KEEP | UPDATE of a pre-existing quota row (limit values). |
| api/webhook_store_gorm.go:746 | GormWebhookQuotaStore.Delete | webhook_quotas DELETE | none | KEEP | DELETE of a pre-existing row. |
| api/webhook_store_gorm.go:850 | GormWebhookUrlDenyListStore.Create | webhook_url_deny_list INSERT | none in closure | OPT_DOWN | Single INSERT of a row whose UUID is generated in this request, no reads. |
| api/webhook_store_gorm.go:869 | GormWebhookUrlDenyListStore.Delete | webhook_url_deny_list DELETE | none | KEEP | DELETE of a pre-existing row. |
| api/document_store_gorm.go:81 | GormDocumentRepository.Create | alias_counters upsert/increment; documents INSERT | alias counter row under row lock only | OPT_DOWN | Single INSERT keyed by a UUID generated in this request plus the row-locked/sequence alias allocation. |
| api/document_store_gorm.go:259 | GormDocumentRepository.update | documents UPDATE (+ optional version CAS) | CheckAndBumpVersion reads/compares version | KEEP | UPDATE of a pre-existing row, optionally CAS-guarded. |
| api/document_store_gorm.go:369 | GormDocumentRepository.hardDeleteDocument | documents DELETE | none | KEEP | Hard DELETE of a pre-existing row. |
| api/document_store_gorm.go:522 | GormDocumentRepository.BulkCreate | alias_counters upsert/increment per doc; documents INSERT per doc | alias counter rows under row lock only | OPT_DOWN | Only INSERTs of rows with UUIDs generated in this request plus row-locked alias allocations. |
| api/document_store_gorm.go:696 | GormDocumentRepository.UpdateAccessStatus | documents UPDATE (access_status, content_source) | none in closure | KEEP | UPDATE of a pre-existing row. |
| api/document_store_gorm.go:755 | GormDocumentRepository.UpdateAccessStatusWithDiagnostics | documents UPDATE (access status + diagnostics) | none in closure | KEEP | UPDATE of a pre-existing row. |
| api/document_store_gorm.go:898 | GormDocumentRepository.SetPickerMetadata | documents UPDATE (picker fields, access_status reset) | none in closure | KEEP | UPDATE of a pre-existing row. |
| api/document_store_gorm.go:940 | GormDocumentRepository.ClearPickerMetadataForOwner | documents UPDATE over a set selected by a threat_models subquery | subquery selects threat model ids by owner, and the UPDATE's row set depends on it | KEEP | Multi-row UPDATE of pre-existing rows whose scope is derived from an ownership subquery. |
| api/document_store_gorm.go:1017 | GormDocumentRepository.updateMetadata | metadata DELETE then INSERT | none | KEEP | Delete-then-insert replacement of existing metadata rows. |
| api/repository_store_gorm.go:93 | GormRepositoryRepository.Create | alias_counters upsert/increment; repositories INSERT | alias counter row under row lock only | OPT_DOWN | Single INSERT keyed by a UUID generated in this request plus the row-locked/sequence alias allocation. |
| api/repository_store_gorm.go:293 | GormRepositoryRepository.Update | repositories UPDATE | none in closure | KEEP | UPDATE of a pre-existing row. |
| api/repository_store_gorm.go:383 | GormRepositoryRepository.hardDeleteRepository | repositories DELETE | none | KEEP | Hard DELETE of a pre-existing row. |
| api/repository_store_gorm.go:513 | GormRepositoryRepository.BulkCreate | alias_counters upsert/increment per repo; repositories INSERT per repo | alias counter rows under row lock only | OPT_DOWN | Only INSERTs of rows with UUIDs generated in this request plus row-locked alias allocations. |
| api/repository_store_gorm.go:744 | GormRepositoryRepository.updateMetadata | metadata DELETE then INSERT | none | KEEP | Delete-then-insert replacement of existing metadata rows. |
| api/note_store_gorm.go:75 | GormNoteRepository.Create | alias_counters upsert/increment; notes INSERT | alias counter row under row lock only | OPT_DOWN | Single INSERT keyed by a UUID generated in this request plus the row-locked/sequence alias allocation. |
| api/note_store_gorm.go:222 | GormNoteRepository.Update | notes UPDATE | none in closure | KEEP | UPDATE of a pre-existing row. |
| api/note_store_gorm.go:306 | GormNoteRepository.hardDeleteNote | notes DELETE | none | KEEP | Hard DELETE of a pre-existing row. |
| api/note_store_gorm.go:561 | GormNoteRepository.updateMetadata | metadata DELETE then INSERT | none | KEEP | Delete-then-insert replacement of existing metadata rows. |

| file:line | function | writes (tables + verb) | reads that feed decisions | class | reason (one sentence) |
|---|---|---|---|---|---|
| api/project_note_store.go:136 | GormProjectNoteStore.Create | project_notes INSERT | none in closure (parent-project existence Count runs outside the tx on s.db) | OPT_DOWN | Closure is a single INSERT of a row keyed by a UUID generated in this request, so no other transaction can touch it. |
| api/project_note_store.go:202 | GormProjectNoteStore.Update | project_notes UPDATE (tx.Save of a pre-read row) | row read outside the tx, then written back wholesale | KEEP | Read-modify-write of an existing row (lost-update window). |
| api/project_note_store.go:221 | GormProjectNoteStore.Delete | project_notes DELETE | RowsAffected drives the not-found result | KEEP | DELETE of a pre-existing row. |
| api/content_feedback_store_gorm.go:56 | GormContentFeedbackRepository.CreateWithTargetCheck | content_feedback INSERT | SELECT id ... FOR UPDATE on the target row; empty result aborts the insert | KEEP | Existence check inside the closure guards the write (row-locked, so likely safe at READ COMMITTED, but it fails rule 2 as written). |
| api/timmy_embedding_store_gorm.go:63 | GormTimmyEmbeddingStore.CreateBatch | timmy_embeddings batch INSERT | none | OPT_DOWN | Pure batch INSERT; TimmyEmbedding.BeforeCreate assigns a fresh UUID per row. |
| api/timmy_embedding_store_gorm.go:83 | GormTimmyEmbeddingStore.DeleteByEntity | timmy_embeddings DELETE | none (RowsAffected only reported) | KEEP | DELETE of pre-existing rows. |
| api/timmy_embedding_store_gorm.go:116 | GormTimmyEmbeddingStore.DeleteByThreatModel | timmy_embeddings DELETE | none | KEEP | DELETE of pre-existing rows. |
| api/timmy_embedding_store_gorm.go:145 | GormTimmyEmbeddingStore.DeleteByThreatModelAndIndexType | timmy_embeddings DELETE | none | KEEP | DELETE of pre-existing rows. |
| api/timmy_embedding_store_gorm.go:228 | GormTimmyEmbeddingStore.DeleteEntitiesWithStaleEmbeddingMetadata | timmy_embeddings DELETE | predicate-driven multi-row delete | KEEP | DELETE of pre-existing rows selected by a stale-metadata predicate. |
| api/project_store_gorm.go:134 | GormProjectStore.Create | projects INSERT, project_responsible_parties INSERT, project_relationships INSERT | users COUNT inside saveResponsibleParties (line 730) branches to InvalidInputError and guards each party INSERT | KEEP | Every write is an insert keyed by the fresh project UUID, but the in-closure user-existence check is a read-derived branch guarding a write (FK-backed, so this is the strongest borderline candidate if the owner wants to relax rule 2). |
| api/project_store_gorm.go:273 | GormProjectStore.update | projects UPDATE, project_responsible_parties DELETE+INSERT, project_relationships DELETE+INSERT, metadata DELETE+INSERT, version CAS | CheckAndBumpVersion CAS, in-tx read-back via getWith | KEEP | Optimistic-lock CAS plus update and delete-then-insert replacement of existing child rows. |
| api/project_store_gorm.go:373 | GormProjectStore.Delete | project_relationships/project_responsible_parties/metadata/projects DELETE | project existence First and threat_models COUNT gate the deletes | KEEP | Check-then-delete cascade over pre-existing rows. |
| api/team_store_gorm.go:134 | GormTeamStore.Create | teams INSERT, team_members INSERT, team_responsible_parties INSERT, team_relationships INSERT, metadata upsert | none in closure (relationship validation runs outside the tx) | OPT_DOWN | All writes are inserts keyed by the fresh team UUID; the only non-plain-insert is saveEntityMetadata's single-statement ON CONFLICT upsert on rows that are themselves new. |
| api/team_store_gorm.go:339 | GormTeamStore.update | teams UPDATE, team_members/team_responsible_parties/team_relationships/metadata DELETE+INSERT | CheckAndBumpVersion CAS, team existence First, in-tx read-back | KEEP | CAS plus update and delete-then-recreate replacement of existing child rows. |
| api/team_store_gorm.go:503 | GormTeamStore.Delete | team_relationships/team_responsible_parties/team_members/metadata/teams DELETE | team existence First and projects COUNT gate the deletes | KEEP | Check-then-delete cascade over pre-existing rows. |
| api/team_note_store.go:136 | GormTeamNoteStore.Create | team_notes INSERT | none in closure (parent-team Count runs outside the tx) | OPT_DOWN | Closure is a single INSERT keyed by a UUID generated in this request. |
| api/team_note_store.go:202 | GormTeamNoteStore.Update | team_notes UPDATE (tx.Save) | row read outside the tx, written back wholesale | KEEP | Read-modify-write of an existing row. |
| api/team_note_store.go:221 | GormTeamNoteStore.Delete | team_notes DELETE | RowsAffected drives not-found | KEEP | DELETE of a pre-existing row. |
| api/survey_response_store_gorm.go:128 | GormSurveyResponseStore.Create | survey_responses INSERT, survey_response_access INSERT x3, groups get-or-create | ensure*Group SELECT-then-INSERT-with-savepoint-recovery resolves the group FK only | OPT_DOWN | Every write is an insert keyed by the fresh response UUID; the built-in-group lookups are FK resolution with duplicate-key race recovery, exactly the pattern already accepted in GormThreatModelStore.Create. |
| api/survey_response_store_gorm.go:411 | GormSurveyResponseStore.update | survey_responses UPDATE, version CAS | CheckAndBumpVersion CAS, RowsAffected not-found | KEEP | UPDATE of an existing row behind an optimistic-lock CAS. |
| api/survey_response_store_gorm.go:481 | GormSurveyResponseStore.Delete | triage_notes/survey_response_access/metadata/survey_answers/survey_responses DELETE | existence check outside the tx | KEEP | Cascading DELETE of pre-existing rows. |
| api/survey_response_store_gorm.go:640 | GormSurveyResponseStore.UpdateStatus | survey_responses UPDATE | status read and transition validated before the tx | KEEP | Workflow state transition UPDATE on an existing row. |
| api/survey_response_store_gorm.go:672 | GormSurveyResponseStore.UpdateAuthorization | survey_response_access DELETE then INSERT | parent row SELECT ... FOR UPDATE, user/group UUID resolution | KEEP | Authorization-bearing delete-then-insert replacement of an existing ACL. |
| api/survey_response_store_gorm.go:780 | GormSurveyResponseStore.SetCreatedThreatModel | survey_responses UPDATE | conditional UPDATE on status plus a read-back that decides success vs error | KEEP | Guarded state transition on an existing row. |
| api/survey_answer_store.go:117 | GormSurveyAnswerStore.ExtractAndSave | survey_answers DELETE then INSERT | none, but the delete precedes the inserts | KEEP | Delete-then-insert replacement of an existing answer set. |
| api/survey_answer_store.go:172 | GormSurveyAnswerStore.DeleteByResponseID | survey_answers DELETE | none | KEEP | DELETE of pre-existing rows. |
| api/timmy_session_store_gorm.go:38 | GormTimmySessionStore.Create | timmy_sessions INSERT | none | OPT_DOWN | Single INSERT; TimmySession.BeforeCreate assigns a fresh UUID. |
| api/timmy_session_store_gorm.go:130 | GormTimmySessionStore.SoftDelete | timmy_sessions UPDATE deleted_at | RowsAffected drives not-found | KEEP | UPDATE of an existing row. |
| api/timmy_session_store_gorm.go:156 | GormTimmySessionStore.UpdateSnapshot | timmy_sessions UPDATE source_snapshot | RowsAffected drives not-found | KEEP | UPDATE of an existing row. |
| api/timmy_session_store_gorm.go:182 | GormTimmySessionStore.UpdateTitle | timmy_sessions UPDATE title | RowsAffected drives not-found | KEEP | UPDATE of an existing row. |
| api/timmy_session_store_gorm.go:247 | GormTimmyMessageStore.Create | timmy_messages INSERT | none in closure (Sequence is computed by the caller outside the transaction) | OPT_DOWN | Single INSERT with a fresh UUID from TimmyMessage.BeforeCreate; the sequence number is decided outside this transaction, so SERIALIZABLE protects nothing here. |
| api/triage_note_store_gorm.go:46 | GormTriageNoteStore.Create | triage_notes INSERT | TriageNote.BeforeCreate (api/models/survey_models.go:147) does an unlocked SELECT MAX(id)+1 per survey response inside this transaction | KEEP | Classic max+1 identity derived from a read and relied on by the insert. |
| api/survey_store_gorm.go:78 | GormSurveyStore.Create | survey_templates INSERT | none (metadata saved after the tx) | OPT_DOWN | Single INSERT keyed by a UUID generated in this request. |
| api/survey_store_gorm.go:186 | GormSurveyStore.Update | survey_templates UPDATE | existing row read before the tx; RowsAffected drives not-found | KEEP | UPDATE of an existing row. |
| api/survey_store_gorm.go:223 | GormSurveyStore.Delete | survey_template_versions DELETE, survey_templates DELETE | RowsAffected drives not-found | KEEP | Cascading DELETE of pre-existing rows. |
| api/survey_store_gorm.go:259 | GormSurveyStore.ForceDelete | triage_notes/survey_response_access/metadata/survey_answers/survey_responses/survey_templates DELETE | correlated subqueries select the rows to delete | KEEP | Multi-table cascading DELETE of pre-existing rows. |
| api/timmy_usage_store_gorm.go:37 | GormTimmyUsageStore.Record | timmy_usage INSERT | none | OPT_DOWN | Single INSERT; TimmyUsage.BeforeCreate assigns a fresh UUID. |

| file:line | function | writes (tables + verb) | reads that feed decisions | class | reason (one sentence) |
|---|---|---|---|---|---|
| api/user_store_gorm.go:171 | GormUserStore.Update | users UPDATE | none (RowsAffected==0 -> not-found) | KEEP | UPDATE of a pre-existing row. |
| api/user_api_quota_store_gorm.go:127 | GormUserAPIQuotaStore.Create | user_api_quotas INSERT | none | OPT_DOWN | Lone INSERT with no read in the closure; uniqueness is enforced by the PK on user_internal_uuid, so no cross-statement snapshot is needed. |
| api/user_api_quota_store_gorm.go:153 | GormUserAPIQuotaStore.Update | user_api_quotas UPDATE | none | KEEP | UPDATE of a pre-existing row. |
| api/user_api_quota_store_gorm.go:187 | GormUserAPIQuotaStore.Delete | user_api_quotas DELETE | none | KEEP | DELETE of a pre-existing row. |
| api/user_api_quota_store_gorm.go:227 | GormUserAPIQuotaStore.Upsert | user_api_quotas INSERT..ON CONFLICT DO UPDATE / MERGE | none (read-back happens outside the tx) | OPT_DOWN | Single-statement atomic upsert, explicitly permitted; the read-back is a separate autocommit statement after the tx. |
| api/group_member_repository.go:270 | GormGroupMemberRepository.AddMember | group_members INSERT | join read of the just-inserted row + users, response-shaping only | OPT_DOWN | Single INSERT of a fresh-UUID row (duplicate caught by the unique constraint); the following join read only populates the response and drives no write. |
| api/group_member_repository.go:352 | GormGroupMemberRepository.RemoveMember | group_members DELETE | none | KEEP | DELETE of pre-existing rows. |
| api/group_member_repository.go:439 | GormGroupMemberRepository.AddGroupMember | group_members INSERT | groups read for the member group's display name, response-shaping only | OPT_DOWN | Single INSERT of a fresh-UUID row; the existence checks run outside the tx and the in-tx read only labels the response. |
| api/group_member_repository.go:492 | GormGroupMemberRepository.RemoveGroupMember | group_members DELETE | none | KEEP | DELETE of pre-existing rows. |
| api/group_repository.go:192 | GormGroupRepository.Create | groups INSERT | none | OPT_DOWN | Lone INSERT of a fresh-UUID group; duplicates are surfaced from the unique constraint, not from a read. |
| api/group_repository.go:208 | GormGroupRepository.Update | groups UPDATE | none | KEEP | UPDATE of a pre-existing row. |
| auth/main.go:170 | rebuildCache | none (writes go to Redis only) | many; whole closure is SELECT-only | KEEP | read-only: already declared `&sql.TxOptions{ReadOnly: true}`, issues no DML and cannot raise ORA-08177; out of scope. |
| auth/repository/deletion_repository.go:41 | GormDeletionRepository.DeleteUserAndData | threat_models/threat_model_access/users + children DELETE and UPDATE (via deleteUserCore) | user lookup, owned threat models, alternate-owner lookup — all decide delete-vs-transfer | KEEP | Cascading multi-table delete/transfer whose writes branch on reads of shared rows. |
| auth/repository/deletion_repository.go:70 | GormDeletionRepository.DeleteUserByInternalUUID | same as above | same as above | KEEP | Same cascading read-then-write deletion path (deleteUserCore). |
| auth/repository/deletion_repository.go:200 | GormDeletionRepository.DeleteGroupAndData | groups/threat_models/threat_model_access/group_members/survey_response_access DELETE | group lookup, built-in-group guard, owned threat models, alternate-user-owner COUNT | KEEP | Cascading delete with a count-then-delete decision and an authorization/built-in-group guard. |
| auth/repository/deletion_repository.go:882 | GormDeletionRepository.TransferOwnership | threat_models UPDATE, threat_model_access upsert, survey responses UPDATE | target/source user existence checks, owned-item lists | KEEP | Read-then-update of pre-existing ownership rows across multiple tables. |
| api/content_token_repository.go:234 | GormContentTokenRepository.DeleteByUserAndProvider | user_content_tokens DELETE | row read whose id feeds the DELETE | KEEP | Read-then-delete of a pre-existing row. |
| api/content_token_repository.go:272 | GormContentTokenRepository.RefreshWithLock | user_content_tokens UPDATE (Save) | SELECT ... FOR UPDATE of the token row, feeds the refresh callback and the write | KEEP | Read-then-update of a pre-existing row; the header comment documents deliberate reliance on SERIALIZABLE retry ordering. |
| auth/linked_identity_store.go:102 | GormLinkedIdentityStore.CreateExclusive | linked_identities INSERT | duplicate-binding SELECT that guards the INSERT | KEEP | Check-then-insert whose whole purpose is the SERIALIZABLE TOCTOU guard. |
| api/settings_service.go:699 | SettingsService.ReEncryptAll | system_settings UPDATE (UpdateColumn per row) | Find of all settings; each value is decrypted and rewritten | KEEP | Read-then-update of every pre-existing row, with a multi-row all-or-nothing key-rotation invariant. |
| api/addon_invocation_quota_store_gorm.go:137 | GormAddonInvocationQuotaStore.Set | addon_invocation_quotas INSERT..ON CONFLICT DO UPDATE / MERGE | none (read-back is outside the tx) | OPT_DOWN | Single-statement atomic upsert, explicitly permitted. |
| api/addon_invocation_quota_store_gorm.go:181 | GormAddonInvocationQuotaStore.Delete | addon_invocation_quotas DELETE | none | KEEP | DELETE of a pre-existing row. |
| api/client_credentials_service.go:101 | ClientCredentialService.Create | client_credentials INSERT | none | OPT_DOWN | The closure ignores `tx` and calls the repo on `s.gormDB`'s own handle, so the transaction is empty and the actual work is a single fresh-UUID INSERT in autocommit — SERIALIZABLE buys nothing here (note: the ignored-tx handle is a latent bug worth filing separately). |
| api/client_credentials_service.go:141 | ClientCredentialService.List | none | SELECT of the owner's credentials | KEEP | read-only (and likewise runs outside the tx handle); out of scope. |
| api/client_credentials_service.go:177 | ClientCredentialService.Delete | client_credentials DELETE (+ best-effort Redis token revoke) | none | KEEP | DELETE of a pre-existing row. |
| api/addon_store_gorm.go:46 | GormAddonStore.Create | addons INSERT | none | OPT_DOWN | Lone INSERT of a fresh-UUID addon row with no reads in the closure. |
| api/addon_store_gorm.go:152 | GormAddonStore.Update | addons UPDATE | none | KEEP | UPDATE of a pre-existing row. |
| api/addon_store_gorm.go:181 | GormAddonStore.Delete | addons DELETE | none | KEEP | DELETE of a pre-existing row. |
| api/addon_store_gorm.go:259 | GormAddonStore.DeleteByWebhookID | addons DELETE | none (RowsAffected only reported) | KEEP | DELETE of pre-existing rows. |
| auth/repository/user_repository.go:220 | GormUserRepository.Create | users INSERT | none | OPT_DOWN | Already opted down (#900): lone fresh-UUID INSERT, passes `&sql.TxOptions{Isolation: sql.LevelReadCommitted}` — no change needed. |
