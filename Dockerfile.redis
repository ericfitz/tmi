# Custom Redis image with security patches
FROM bitnami/redis:latest

# Update and patch the base image
USER root
RUN apt-get update && apt-get upgrade -y && apt-get clean && rm -rf /var/lib/apt/lists/*
USER 1001