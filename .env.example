# Server configuration
PORT=4200
HOST=localhost
NODE_ENV=development  # development or production
ENV=development  # legacy - use NODE_ENV instead

# Auth configuration
# AUTH_PROVIDER=anonymous  # anonymous or google

# Logging
# LOG_LEVEL=debug  # error, warn, info, debug, trace