# Websocket Test Harness

This directory contains the websocket test harness tool used for testing the TMI websocket connection and messages.

The compiled binary of the tool is currently called wstest; it was previously called ws-test-harness
